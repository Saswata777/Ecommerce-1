import React from 'react'

export const cartReducer = (state, action) => {
  switch (action.type) {
    
  
    default:
      return state;
  }

}



const reducer = () => {
  return (
    <div>reducer</div>
  )
}

export default reducer